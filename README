
Procedure to create a new GITHUB repository

1. cd /Users/John/Development/exports/Couchbase
2. git init
3. git add *
4. git commit -m "Couchbase"
5. git remote add origin https://github.com/johnrnoone/Couchbase.git
6. git push -u origin master

To add additional files to that repo repeat these steps except skip step 5.

To modify a file and post it to the remote GitHub repository:

> git add README
> git commit -m "updated README"
> git remote add origin https://github.com/johnrnoone/JohnNooneGitRepository.git
> git push -u origin master


Note: To undo any file changes since the last commit: git reset --hard
Note: To remove any added files and folder since the last commit: git clean -d -f
Note: To change the last commit: git commit --amend

